import { appendHTML, createHTML } from '../../helper.js';

export function pageReports(){
    const pageReports = createHTML("div", {class: "page page-reports"}, "Page Reports");

    return pageReports
}