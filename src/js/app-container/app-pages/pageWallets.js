import { appendHTML, createHTML } from '../../helper.js';

export function pageWallets(){
    const pageWallets = createHTML("div", {class: "page page-wallets"}, "Page Wallets");

    return pageWallets
}